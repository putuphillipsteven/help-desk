import MainTeam from '@/app/features/dashboard/agents/components/main-team';

export default function Layout({ children }: { children: React.ReactNode }) {
	return <MainTeam children={children} />;
}
